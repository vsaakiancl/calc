---
phase: 01-core-engine-basic-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - src/index.css
  - tsconfig.json
  - vitest.config.ts
  - index.html
  - src/main.tsx
  - src/App.tsx
autonomous: true
requirements:
  - CORE-01
  - CORE-02
  - CORE-03
  - CORE-04

must_haves:
  truths:
    - "npm run dev starts a Vite dev server without errors"
    - "npm run test runs Vitest and exits cleanly (no tests yet = zero failures)"
    - "Tailwind v4 is configured and the @import directive is in index.css"
    - "All runtime and dev dependencies are installed at specified versions"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with all dependencies declared"
      contains: "mathjs"
    - path: "vite.config.ts"
      provides: "Vite build config with Tailwind v4 plugin and Vitest config"
      exports: ["default"]
    - path: "vitest.config.ts"
      provides: "Vitest jsdom environment config for component tests"
      contains: "jsdom"
    - path: "src/index.css"
      provides: "Tailwind v4 entry point"
      contains: "@import \"tailwindcss\""
  key_links:
    - from: "vite.config.ts"
      to: "@tailwindcss/vite"
      via: "plugins array"
      pattern: "tailwindcss\\(\\)"
    - from: "vitest.config.ts"
      to: "jsdom"
      via: "environment setting"
      pattern: "environment.*jsdom"
---

<objective>
Scaffold the React/Vite project with all required dependencies and test infrastructure configured.

Purpose: Everything downstream — engine, hook, components — runs inside this scaffold. Tests require Vitest + jsdom. Tailwind must be installed now so Phase 3 can add utility classes without restructuring.

Output: A working Vite dev server, a passing `npm run dev`, a Vitest config targeting jsdom, and a minimal App.tsx shell (no calculator logic yet).
</objective>

<execution_context>
@/Users/volodymyrsaakian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/volodymyrsaakian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-core-engine-basic-ui/01-CONTEXT.md
@.planning/phases/01-core-engine-basic-ui/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scaffold Vite project, install dependencies, configure Tailwind v4</name>
  <read_first>
    .planning/phases/01-core-engine-basic-ui/01-RESEARCH.md (Standard Stack section, Installation block, Tailwind v4 Vite config block)
  </read_first>
  <files>
    package.json, vite.config.ts, src/index.css, index.html, src/main.tsx, src/App.tsx, tsconfig.json
  </files>
  <action>
Run the following commands from the project root (`/Users/volodymyrsaakian/Documents/Work/Projects/mugco/calc`):

```bash
npm create vite@latest . -- --template react-ts --force
npm install mathjs clsx
npm install tailwindcss @tailwindcss/vite
npm install -D vitest @vitest/ui jsdom @testing-library/react @testing-library/jest-dom
```

After scaffolding, update `vite.config.ts` to add the Tailwind plugin:

```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [
    react(),
    tailwindcss(),
  ],
})
```

Replace the contents of `src/index.css` with exactly:

```css
@import "tailwindcss";
```

Replace `src/App.tsx` with a minimal shell (no calculator logic — just a placeholder):

```tsx
function App() {
  return (
    <div className="min-h-screen bg-gray-900 flex items-center justify-center">
      <p className="text-white">Calculator loading…</p>
    </div>
  )
}

export default App
```

Do NOT add trig, history, or glassmorphism content here (Phase 2 and 3 scope).
  </action>
  <verify>
    <automated>cd /Users/volodymyrsaakian/Documents/Work/Projects/mugco/calc && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"mathjs"` as a dependency key
    - package.json contains `"tailwindcss"` as a dependency key
    - package.json contains `"@tailwindcss/vite"` as a dependency key
    - package.json contains `"vitest"` as a devDependency key
    - package.json contains `"jsdom"` as a devDependency key
    - package.json contains `"@testing-library/react"` as a devDependency key
    - vite.config.ts contains the string `tailwindcss()`
    - vite.config.ts contains the string `from '@tailwindcss/vite'`
    - src/index.css contains exactly `@import "tailwindcss"`
    - `npm run build` exits 0 with no errors
  </acceptance_criteria>
  <done>Vite project is scaffolded, all dependencies installed, Tailwind v4 configured, build passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Configure Vitest with jsdom environment</name>
  <read_first>
    vite.config.ts (just written in Task 1 — check if Vitest config can be co-located or needs separate file)
    .planning/phases/01-core-engine-basic-ui/01-VALIDATION.md (Wave 0 requirements, test commands)
    .planning/phases/01-core-engine-basic-ui/01-RESEARCH.md (Validation Architecture section)
  </read_first>
  <files>
    vitest.config.ts, package.json
  </files>
  <action>
Create `vitest.config.ts` at the project root with jsdom environment and @testing-library/jest-dom setup:

```typescript
import { defineConfig } from 'vitest/config'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    setupFiles: ['./src/test-setup.ts'],
    globals: true,
  },
})
```

Create `src/test-setup.ts`:

```typescript
import '@testing-library/jest-dom'
```

Add the following scripts to `package.json` (merge with existing scripts, do not replace other scripts):

```json
"test": "vitest run",
"test:watch": "vitest",
"test:ui": "vitest --ui"
```

Verify the test script runs without crashing (zero tests = zero failures is acceptable):

```bash
npm test
```
  </action>
  <verify>
    <automated>cd /Users/volodymyrsaakian/Documents/Work/Projects/mugco/calc && npm test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - vitest.config.ts exists at project root
    - vitest.config.ts contains the string `environment: 'jsdom'`
    - vitest.config.ts contains the string `setupFiles`
    - src/test-setup.ts exists and contains `import '@testing-library/jest-dom'`
    - package.json scripts object contains key `"test"` with value `"vitest run"`
    - `npm test` exits 0 (zero tests found is not an error)
  </acceptance_criteria>
  <done>Vitest configured with jsdom; `npm test` exits 0; test-setup.ts loads jest-dom matchers.</done>
</task>

</tasks>

<verification>
Run the full suite from project root:

```bash
cd /Users/volodymyrsaakian/Documents/Work/Projects/mugco/calc
npm run build    # must exit 0
npm test         # must exit 0
```

Confirm vite.config.ts, vitest.config.ts, and src/index.css are all present with the expected content.
</verification>

<success_criteria>
- `npm run dev` starts without errors
- `npm run build` exits 0
- `npm test` exits 0 (no test failures; zero tests is acceptable at this stage)
- package.json declares: mathjs, clsx, tailwindcss, @tailwindcss/vite, vitest, jsdom, @testing-library/react, @testing-library/jest-dom
- vite.config.ts uses @tailwindcss/vite plugin
- src/index.css contains `@import "tailwindcss"` and nothing else
- vitest.config.ts targets jsdom environment
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-engine-basic-ui/01-01-SUMMARY.md`
</output>
